{
  "name": "groway_landing",
  "version": "1.0.0",
  "description": "groway.studio landing",
  "main": "index.js",
  "scripts": {
    "prebuild": "run-s clean",
    "build": "parcel build src/index.html --no-source-maps",
    "clean": "rimraf ./dist",
    "start": "parcel index.html --open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "groway",
    "studio",
    "landing"
  ],
  "author": "groway studio",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
